# Подхватывается автоматически при `npm run dev` (можно лежать в репозитории).
# Локальные правки в .env.local имеют ПРИОРИТЕТ — не оставляйте там пустой NEXTAUTH_SECRET= !
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=compare-dev-builtin-not-for-prod
COMPARE_SKIP_AUTH=1
