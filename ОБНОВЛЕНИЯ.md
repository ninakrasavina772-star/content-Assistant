# Журнал обновлений (ассистент контент)

Короткий список, чтобы **видеть, что менялось** без копания в чате.  
Агент в Cursor по правилу проекта дописывает сюда строки после важных правок.

| Дата | Что сделано |
|------|-------------|
| 2026-04-24 | Объём: если в характеристиках нет — выдёргиваем из названия/описания (`extractVolumePhraseFromText`); сравнение в `applyAttrGate` и «семействе ссылок» через `volumeStringsEquivalentForGate` (50 мл ≈ 50ml). |
| 2026-04-24 | «Маловероятные» дубли: одно фото (URL) + сходство модельной части ≥60% (`scoreUnlikelyPhotoAndModel`); нормализация объёма (50 мл ≈ 50ml); галочки характеристик — подписи, не гейт. |
| 2026-04-24 | Сравнение: блок «Что значат сайт A и сайт B», подсказки у полей ключей/подписей, уточнение в блоке рубрик. |
| 2026-04-24 | Сравнение: исключение товаров **сайта A** по списку id (текст/Excel) до брендов и моделей; `excludeIdsA` в ответе, блоки в отчёте. |
| 2026-04-24 | Сравнение: список моделей (столбик/файл), фильтр каталога по вхождению или точному совпадению; `modelFilter` в ответе API, блок в отчёте. |
| 2026-04-24 | ProductCell: кнопка «Админка» → `https://4stand.com/A`+id (опц. `NEXT_PUBLIC_4STAND_ADMIN_BASE`). |
| 2026-04-24 (позже) | Режим «одна рубрика»: счётчики в шапке (всего блоков EAN+имя+фото+маловер.), в подписи фильтра и на кнопках; числа в заголовках секций. |
| 2026-04-24 (позже) | Режим дублей: в ответ API добавлен `unlikelySearch` (была ли попытка искать «фото+хар.»), подсказки в UI — отличие «не искали» / «0 совпадений»; то же для сравнения двух витрин. |
| 2026-04-24 | Сравнение: ускорен поиск внутри рубрики (`intraSiteDups` — пары только внутри бренда); подсказка при «Загрузка…», что вкладка не зависла. |
| 2026-04-23 | Единая стилистика главной (`homeTheme.ts`), карточки, журнал + правило «пошагово». |
| 2026-04-23 | Единая оболочка подстраниц (`AssistantSubpageShell`, `homeTheme`): размеры/цены, сравнение; светлая тема iframe `assistant-embed.css` для обменника и фона сетки. |
| 2026-04-23 | Сравнение: `AbortController`, тайм‑аут 30 мин, кнопка «Отменить», счётчик «Идёт запрос…» — чтобы кнопка не оставалась в «Загрузка…» бесконечно. |
| 2026-04-23 (раньше) | Сравнение: кнопка, рубрики, прямой id; обменник в `/prices-stock`; размерная сетка в `/size-table`; каскад рубрик одним полем. |

**Как пользоваться вам:** откройте этот файл в проекте — сверху новые даты. Можно закрепить вкладку в IDE или добавить в «Избранное».

**Напоминание агенту:** после существенных изменений в `compare/` — одна новая строка в таблицу выше (дата = из User Info / сегодня).
